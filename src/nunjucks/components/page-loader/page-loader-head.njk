<!-- ▼▼▼ ページローダーCSS ▼▼▼ -->
<style>
.loader {
  position: fixed;
  display: flex;
  width: 100%;
  height: 100%;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  background: #02a2f7;
  justify-content: center;
  align-items: center;
  z-index: 10000;
}
.loader__content {
  width: 240px;
}
</style>
<!-- ▲▲▲ ページローダーCSS ▲▲▲ -->

{# 「gulp-htmlmin」での圧縮が効かなかったため、元の記述をVScode側で圧縮しておく #}
{# <script>
  const loaderEl = document.getElementById('js-loader');
  if (loaderEl) {
    document.documentElement.style.overflow = 'hidden';
    setTimeout(() => {
      const loaderElDisplay = document.defaultView.getComputedStyle(loaderEl, null).display;
      if (loaderElDisplay === 'flex') {
        loaderEl.style.display = 'none';
        document.documentElement.style.overflow = null;
      }
    }, 10000);
  }
</script> #}

<!-- ▼▼▼ ページローダーJS ▼▼▼ -->
<script>const loaderEl=document.getElementById('js-loader');if(loaderEl){document.documentElement.style.overflow='hidden';setTimeout(()=>{const loaderElDisplay=document.defaultView.getComputedStyle(loaderEl,null).display;if(loaderElDisplay==='flex'){loaderEl.style.display='none';document.documentElement.style.overflow=null}},10000)}</script>
<!-- ▲▲▲ ページローダーJS ▲▲▲ -->